# Documentation toolchain for the Barosense site.
#
# Pinned to minor versions: the site is built in CI on a clean runner, and an
# unpinned Material release has changed nav/`hooks` behaviour between minors
# before. Bump deliberately, in its own commit, with the built site checked.
mkdocs==1.6.1
mkdocs-material==9.6.14
# `mkdocs-material[imaging]` pulls cairo/pango via Homebrew, which the CI image
# does not have. Social cards are off, so the extra is not requested.
pymdown-extensions==10.15
mkdocs-literate-nav==0.6.2
mkdocs-glightbox==0.4.0
mkdocs-minify-plugin==0.8.0
